import { StreetLegalPipe } from './street-legal.pipe';

describe('StreetLegalPipe', () => {
  it('create an instance', () => {
    const pipe = new StreetLegalPipe();
    expect(pipe).toBeTruthy();
  });
});
